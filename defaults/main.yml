---
# defaults file for sa-mongo
  root_dir: "{{role_path}}"
  mongo_version: "3"  #  "2.6" | "3"
  mongo_bind_ip: 127.0.0.1
  mongo_conf_properties:
    - {regexp: "^bind_ip =.*", line: "bind_ip = {{mongo_bind_ip}}"}
